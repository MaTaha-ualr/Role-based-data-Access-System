# Role-based-data-Access-System
A Python-based access control system that restricts user access to sensitive data columns based on predefined permissions. Uses CSV files for data storage and role-based access control.
